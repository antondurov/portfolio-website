{
  "name": "portfolio-website",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "bun run --cwd ./frontend test && bun run --cwd ./backend test",
    "lint": "bun run --cwd ./frontend lint && bun run --cwd ./backend lint"
  },
  "workspaces": ["backend", "packages", "frontend"],
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^24.13.2",
    "lefthook": "^2.1.9",
    "typescript": "~6.0.2"
  },
  "engines": {
    "node": "24.14.0",
    "npm": "11.9.0"
  },
  "dependencies": {
    "eslint": "^10.9.0"
  }
}
